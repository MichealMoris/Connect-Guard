<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mainDarkGreen</item>
        <item name="colorPrimaryDark">@color/mainDarkGreen</item>
        <item name="colorAccent">@color/mainPink</item>
    </style>

    <style name="ThemeSpinner">
        <!-- Default color for the dropdown arrow and line -->
        <item name="colorControlNormal">@color/mainDarkGreen</item>
    </style>

    <!-- Admin Main -->
    <style name="AdminMainText">

        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textStyle">bold</item>

    </style>
    <style name="AdminAddET">

        <item name="android:layout_width">280dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginStart">50dp</item>
        <item name="android:layout_marginEnd">50dp</item>
        <item name="android:background">@drawable/edittext_background_blue</item>

    </style>


</resources>